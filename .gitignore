# deps + build
node_modules/
dist/
.astro/
.wrangler/

# env
.env
.env.local
.env.*.local

# sqlite + research caches (regenerable from scrape/research steps)
pipeline.db
pipeline.db-journal
research/

# macOS
.DS_Store

# bootstrap resource-ID cache (rebuildable via discover(); may contain secrets)
bootstrap-refs.yaml
